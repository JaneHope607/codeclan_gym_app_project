<a href="/bookings/new">Create New Booking</a>

<h1>All Bookings</h1>

    <table>
        <tr>
            <th>Booking Reference</th>
            <th>Member Name</th>
            <th>Class</th>
            <th>Day</th>
            <th>Session Time</th>
        </tr>
        <hr/>
        <% for booking in @bookings%>
        <tr>
            <td><%= booking.id%></td>
            <td><%= booking.member.full_name()%></td>
            <td><%= booking.session.gym_class.name%></td>
            <td><%= booking.session.day%></td>
            <td><%= booking.session.start_time%></td>
            <td><form method="post" action="/bookings/<%= booking.id%>/delete">
                    <input type="submit" value="Delete Booking"/>
                </form></td>
        <% end %>
        </tr>
    </table>


