<h3 class="add">New Booking:</h3>

<form autocomplete="off"method="post" action="/bookings">
    <div class="form-wrapper">

        <div class="form-el">   
            <label for="member">Select A Member:</label>
            <select name="member_id" id="member_id">
                <% for member in @members%>
                    <option required value="<%= member.id%>"><%= member.full_name()%></option>
                <% end %>
            </select>
        </div>

        <div class="form-el">   
            <label for="session">Select Available Class:</label>
            <select name="session_id" id="session_id">
                <% for session in @sessions%>
                    <% if session.members.count < session.capacity %>
                        <option required value="<%=session.id%>"> <%= session.gym_class.name%> - <%=session.get_day()%> @ <%=session.start_time%></option>
                    <% end %>
                <% end %>
            </select>
        </div>

        <div class="fomr-el">   
            <input class="btn" type="submit" value="Create Booking"/>
        </div>

    </div>
</form>